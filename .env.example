# Default Nostr relays
DEFAULT_RELAYS=wss://relay.damus.io,wss://relay.nostr.band,wss://nos.lol
# Timeout for relay connections in milliseconds
RELAY_TIMEOUT=3000
# Port for the Express server
PORT=3000